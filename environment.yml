name: taller-ia
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  # Core scientific computing
  - numpy>=1.24,<2.0
  - matplotlib>=3.7
  # Reinforcement Learning
  - gymnasium>=1.0
  - pygame>=2.1
  # Jupyter environment
  - jupyter
  - ipython
  - notebook
  - ipykernel
  # Progress bars
  - tqdm
  # Utilities
  - pip:
    # Any pip-only packages can go here if needed
